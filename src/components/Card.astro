
---
const { name, lastName, gender, age, picture } = Astro.props;



---

<div class=" bg-white rounded-xl shadow-md border  mt-8">
	<div class="p-6 flex flex-col items-center">
		<div class="w-20 h-20  rounded-full flex items-center justify-center mb-4 overflow-hidden">
			<img src={picture} alt="Profile picture" class="w-20 h-20 object-cover rounded-full" />
		</div>
		<h2 class="text-xl font-semibold ">{name} {lastName}</h2>
		<p class=" mt-1">Gender: <span class="font-medium ">{gender}</span></p>
		<p class="">Age: <span class="font-medium ">{age}</span></p>
	</div>
</div>
